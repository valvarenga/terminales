<div class="row g-3">
    <div class="col-md-6"><label for="name" class="form-label">Nombre</label><input id="name" name="name" class="form-control" maxlength="255" value="{{ old('name', $user->name ?? '') }}" required></div>
    <div class="col-md-6"><label for="email" class="form-label">Correo para iniciar sesión</label><input type="email" id="email" name="email" class="form-control" maxlength="255" value="{{ old('email', $user->email ?? '') }}" autocomplete="off" required></div>
    <div class="col-md-6"><label for="role" class="form-label">Permisos</label><select id="role" name="role" class="form-select">@foreach(['editor' => 'Editor', 'admin' => 'Administrador'] as $value => $label)<option value="{{ $value }}" @selected(old('role', $user->role ?? 'editor') === $value)>{{ $label }}</option>@endforeach</select></div>
    <div class="col-md-6 d-flex align-items-center"><div class="form-check mt-3"><input type="hidden" name="is_active" value="0"><input class="form-check-input" type="checkbox" id="is_active" name="is_active" value="1" @checked(old('is_active', $user->is_active ?? true))><label for="is_active" class="form-check-label">Cuenta activa</label></div></div>
    <div class="col-md-6"><label for="password" class="form-label">{{ isset($user) ? 'Nueva contraseña (opcional)' : 'Contraseña' }}</label><input type="password" id="password" name="password" class="form-control" minlength="12" maxlength="128" autocomplete="new-password" @required(!isset($user))><p class="form-text mb-0">Mínimo 12 caracteres, con letras y números.</p></div>
    <div class="col-md-6"><label for="password_confirmation" class="form-label">Repetir contraseña</label><input type="password" id="password_confirmation" name="password_confirmation" class="form-control" autocomplete="new-password" @required(!isset($user))></div>
</div>
